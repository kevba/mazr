const initialState = {
    width: 800,
    height: 800,
    scale: 10
};

let canvas = function(state = initialState, action) {
    switch (action.type) {
    default:
        return state;
    }
};

export default canvas;
